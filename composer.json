{
    "name": "telegram-movie-bot",
    "description": "PHP based Telegram Movie Bot for Render.com",
    "type": "project",
    "require": {
        "php": ">=8.1"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "scripts": {
        "post-install-cmd": [
            "chmod 664 movies.csv users.json",
            "chmod 775 uploads logs"
        ]
    },
    "minimum-stability": "stable",
    "prefer-stable": true
}
